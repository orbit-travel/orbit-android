<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="1.5dp">

    <ImageView
        android:id="@+id/galleryImage"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/orbit_surface_light"
        android:contentDescription="@string/section_photos"
        android:scaleType="centerCrop"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/gallerySelectionScrim"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#5564D2FF"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/galleryImage"
        app:layout_constraintEnd_toEndOf="@id/galleryImage"
        app:layout_constraintStart_toStartOf="@id/galleryImage"
        app:layout_constraintTop_toTopOf="@id/galleryImage" />

    <ImageView
        android:id="@+id/gallerySelectionBadge"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_margin="5dp"
        android:background="@drawable/bg_selection_badge"
        android:contentDescription="@string/gallery_picker_done"
        android:padding="3dp"
        android:src="@drawable/ic_check"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="@id/galleryImage"
        app:layout_constraintTop_toTopOf="@id/galleryImage" />
</androidx.constraintlayout.widget.ConstraintLayout>
